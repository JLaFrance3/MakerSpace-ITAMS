import { InventoryItem } from "../models/inventory_item";

export function getItem(id: number | null) {
    if (typeof id === "number") {
        //return statement
    } else {
        // return statement
    }
}